# recipe-box

A tiny command-line recipe manager. Recipes live in a local `recipes.json` file.

## Usage

    node src/cli.js add "Carbonara" --ingredients "eggs,guanciale,pecorino"
    node src/cli.js list
    node src/cli.js find eggs
